{
  "permissions": {
    "defaultMode": "bypassPermissions",
    "deny": [
      "Bash(git push origin main)",
      "Bash(git push origin staging)",
      "Bash(rm -rf *)",
      "Bash(git reset --hard *)",
      "Bash(sudo *)",
      "Bash(* --force)"
    ]
  }
}
